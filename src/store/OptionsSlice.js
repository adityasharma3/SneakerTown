import { createSlice } from "@reduxjs/toolkit";

const optionSlice = createSlice({
    name: 'options',
    initialState: {
        options: [],
    },

    reducers: {
        findTotalOptions(state, actions) {
            const newItem = 
        }
    },
});